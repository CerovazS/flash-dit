# Entry point redirect — use scripts/ for training and inference.
# Usage: uv run python scripts/train.py   (training)
#         uv run python scripts/sample.py  (inference)
print("flash-dit: use scripts/train.py or scripts/sample.py")
